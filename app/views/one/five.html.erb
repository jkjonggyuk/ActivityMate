<!DOCTYPE html>
<html>
	<head>
		<title>
			Registration Page
		</title>	
	</head>

	<body>
		<%= render 'layouts/header' %>
		<div class="container">
			<%= yield%>
			<%=render 'layouts/footer' %>
			#restricts debug info to develop. environment
			<%= debug(params) if Rails.env.development? %>

		</div>
	</body>

</html>

